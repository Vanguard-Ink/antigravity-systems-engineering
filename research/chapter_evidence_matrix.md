# Master Evidence & Ground Truth Matrix: Chapters 1 – 14

This matrix serves as the invariant contract for the monograph. Every chapter must rigorously integrate the specified standards, APIs, empirical evidence, and failure labs.

| Ch # | Chapter Title & Question | Systems Invariant / Ground Truth | Kernel / POSIX Standards & APIs | Empirical Case Study / Evidence | Failure Lab & Git Tag |
| :---: | :--- | :--- | :--- | :--- | :--- |
| **01** | **When the Agent Holds the Keyboard**<br>*Why does terminal access break security?* | Shell execution inherits ambient authority by default. Probabilistic models cannot guard deterministic execution boundaries. | POSIX.1-2024 `fork()`, `execve()`, `/proc/$PID/fd`, `/proc/$PID/environ`, `env -i`. | CVE-2023-36258 (LangChain RCE), Greshake et al. (2023) Indirect Injection. | Lab 01: Ambient `.env` leak via injected markdown command.<br>`ch01-shell-vulnerability` |
| **02** | **Systems Anatomy of Agent Execution**<br>*What happens between prompt and OS?* | Shell pipes without asynchronous drain create kernel buffer deadlocks. Unsupervised PTYs accumulate state drift. | `posix_openpt(3)`, `termios`, `/proc/sys/fs/pipe-max-size` (64KB buffer), `waitpid(2)`, `SIGCHLD`. | CWE-403 (FD leakage), zombie process PID table exhaustion incidents in production daemons. | Lab 02: Interactive subshell prompt hanging process and exhausting PIDs.<br>`ch02-process-anatomy` |
| **03** | **Sandbox Internals & Process Virtualization**<br>*How to jail an agent without hypervisor overhead?* | Lightweight rootless sandboxing via unshare/bwrap achieves <30ms startup while constraining kernel attack surface. | Linux Namespaces (`CLONE_NEW*`), cgroups v2 (`memory.max`, `pids.max`), Seccomp-BPF (`struct sock_fprog`), OverlayFS. | Fork-bomb attacks (`:(){ :|:& };:`), Dirty COW / container breakout CVEs. | Lab 03: Fork-bomb & disk-fill containment under cgroups v2.<br>`ch03-sandbox-internals` |
| **04** | **Permissions, Boundaries, and Command Allowlists**<br>*How to balance agent problem-solving with safety?* | Regex blacklists fail due to shell syntax evasion. Allowlisting requires Abstract Syntax Tree (AST) tokenization. | POSIX shell tokenization, `tree-sitter-bash`, `PATH` resolution, Linux capabilities (`cap_drop`). | Regex bypass via IFS manipulation, base64 pipes, and quote splitting. | Lab 04: Blacklist evasion bypassed by AST parser allowlist.<br>`ch04-permissions-allowlist` |
| **05** | **Ephemeral Environments and State Management**<br>*How to guarantee clean execution states?* | Workspaces must be disposable Copy-on-Write instances. Diffs must be audited before merging to baseline. | OverlayFS (`lowerdir`, `upperdir`, `workdir`), Btrfs/ZFS snapshots, `git worktree add --detach`. | Accidental dependency purge (`rm -rf node_modules /`) during agent build step. | Lab 05: Workspace corruption recovery via zero-latency CoW rollback.<br>`ch05-ephemeral-environments` |
| **06** | **Threat Modeling for Autonomous Coding Agents**<br>*What new attack surfaces emerge with autonomous reasoning?* | Autonomous tool loops expand attack surface from human-assisted RCE to automated lateral traversal. | STRIDE taxonomy, DREAD risk scoring, MITRE ATT&CK for Enterprise. | Slopsquatting (Lanyado, 2024), PyPI/npm hallucinated package malicious registration. | Lab 06: Interception of hallucinated package callback in sandbox.<br>`ch06-threat-modeling` |
| **07** | **Indirect Prompt Injection Defense**<br>*How to neutralize attacks hidden in passive context?* | Control plane instructions must be cryptographically and structurally separated from untrusted data plane strings. | Context fencing, structured tool schemas (JSON-RPC), Unicode zero-width character sanitization. | CVE-2024-5184 (PR description markdown injection causing table drop). | Lab 07: CI bug-fixer lured into destructive DB command neutralized by fence.<br>`ch07-indirect-injection` |
| **08** | **Deterministic Guardrails & Interception Shims**<br>*Why replace LLM judges with binary shims?* | LLM-as-a-judge adds seconds of latency and is bypassable. Binary shims enforce sub-millisecond deterministic blocking. | Dynamic linker preloading (`LD_PRELOAD`), Linux PATH interception wrappers, eBPF `kprobe` / `lsm`. | Token cost inflation and prompt-jailbreak bypasses of LLM guardrails. | Lab 08: Interception shim catches and terminates obfuscated `base64 \| sh`.<br>`ch08-deterministic-shims` |
| **09** | **Network Egress & Secret Isolation**<br>*How to allow package installs without token leakage?* | Default-drop network namespaces with credential proxy brokering prevent data exfiltration. | Netfilter / `iptables`, Linux network namespaces (`veth`), DNS sinkholing, Canary tokens. | Exfiltration of AWS keys via DNS tunneling query payloads. | Lab 09: Blocking unauthorized reverse TCP shell and DNS exfiltration.<br>`ch09-secrets-network` |
| **10** | **Headless Agent Architecture & Daemon Operations**<br>*How to run background agents reliably 24/7?* | Unattended daemons require hardware/systemd watchdog heartbeats and dead-man switches. | Systemd service units, `sd_notify(0, "WATCHDOG=1")`, Linux OOM killer tuning (`oom_score_adj`). | Zombie agent reasoning loops burning thousands of API tokens unattended. | Lab 10: Unattended webhook issue resolver daemon with systemd watchdog.<br>`ch10-headless-daemon` |
| **11** | **State Persistence & Agent Checkpoints**<br>*How to prevent multi-hour tasks from crashing midway?* | State of Mind (token sequence) and State of World (git tree & filesystem diff) must be serialized in lockstep. | SQLite WAL (Write-Ahead Log), Git commit SHA DAG, JSON-RPC event log rehydration. | Host machine power failure or kernel panic midway through 3-hour refactor. | Lab 11: Mid-flight `kill -9` recovery from durable checkpoint log.<br>`ch11-state-checkpoints` |
| **12** | **Inter-Process Communication & Fleet Orchestration**<br>*How do supervisors communicate with isolated workers?* | UNIX domain sockets provide authenticated (`SO_PEERCRED`), zero-copy, capability-passing (`SCM_RIGHTS`) IPC. | `AF_UNIX`, `SOCK_STREAM`, `sendmsg(2)`, `struct ucred`, file descriptor passing. | Unauthorized process hijacking open loopback TCP ports on shared host. | Lab 12: Host dispatcher orchestrating 3 sandboxes via UNIX domain sockets.<br>`ch12-ipc-orchestration` |
| **13** | **Capstone Case Study — The Autonomous SRE Daemon**<br>*How do isolation, containment, and daemons unite?* | Production incident remediation requires read-only live introspection paired with isolated replica simulation. | Prometheus webhook listener, ephemeral sandbox, git bisect automation, memory profilers. | 02:00 AM production memory leak incident causing HTTP 500 error storms. | Lab 13: End-to-end Autonomous SRE reproducing leak, patching, and opening PR.<br>`ch13-autonomous-sre` |
| **14** | **The Dawn of the Agentic Operating System**<br>*How must OS design evolve for machine users?* | POSIX shell text streams are a historic bridge. Future OS primitives must be capability-based and cryptographically attested. | Capabilities-based OS (Capsicum, Fuchsia), WASI 0.2 component model, SPIFFE/SPIRE agent attestation. | Architectural critique of 50-year-old POSIX assumptions in AI-native compute. | Lab 14: Prototype capability-attested microkernel agent sandbox.<br>`ch14-agentic-os` |
